export interface IMTBItem {
  code: {
    _text: string;
  };
  codeTo: {
    _text: string;
  };
  purchase: {
    _text: string;
  };
  sale: {
    _text: string;
  };
  cacheless: {
    _text: string;
  };
}
